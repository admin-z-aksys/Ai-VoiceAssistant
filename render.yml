services:
  - type: web
    name: ai-voiceassistant
    env: python
    plan: standard
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: PORT=8000 uvicorn api.app:app --host 0.0.0.0 --port $PORT
    packages:
      - espeak-ng
